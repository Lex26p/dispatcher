# v1.5.0 — Modbus TCP telemetry adapter

## Summary

This release introduces the first concrete telemetry adapter foundation for Dispatcher: a Modbus TCP adapter library.

The new backend library `dispatcher-modbus` provides Modbus TCP frame encoding/decoding, register value decoding, a transport abstraction, a protocol-level client, polling configuration and read-plan generation, a single-cycle telemetry adapter, and an in-memory simulated transport for smoke testing.

This release does not yet include a production TCP socket transport and does not yet connect the adapter to `DispatcherRuntime`. It provides the protocol and adapter foundation for future runtime integration.

## Release tag

```text id="tc97s4"
v1.5.0-modbus-adapter
```

## Scope

This release introduces:

```text id="qjwt4x"
dispatcher-modbus backend library
ModbusError
Modbus protocol types
ModbusFrameCodec
ModbusRegisterDecoder
IModbusTcpTransport
ModbusTcpClient
Modbus polling configuration
Modbus polling read-plan builder
ModbusTelemetryAdapter
ModbusSimulatedTcpTransport
Modbus adapter documentation
Modbus protocol and adapter tests
```

## Library

New library:

```text id="rvybqs"
backend/libs/dispatcher-modbus
```

CMake target:

```text id="l1t8oy"
dispatcher-modbus
```

Umbrella header:

```cpp id="y4iupm"
#include <dispatcher/modbus/modbus.hpp>
```

The library is registered in:

```text id="lcqw7g"
backend/CMakeLists.txt
```

The library currently has no third-party runtime dependency.

## Core protocol types

Core protocol types were added in:

```text id="bj2g83"
backend/libs/dispatcher-modbus/include/dispatcher/modbus/modbus_types.hpp
```

Main types:

```text id="au3mcs"
ModbusBytes
ModbusFunctionCode
ModbusReadRegistersRequest
ModbusReadRegistersResponse
ModbusExceptionResponse
```

Supported read function codes:

```text id="qs9l3h"
0x03 — read_holding_registers
0x04 — read_input_registers
```

The enum also defines:

```text id="m65sym"
0x01 — read_coils
0x02 — read_discrete_inputs
```

but this release does not yet implement coil or discrete input polling.

## ModbusError

Modbus-specific errors are represented by:

```text id="rce3p7"
dispatcher::modbus::ModbusError
```

File:

```text id="w0k4l5"
backend/libs/dispatcher-modbus/include/dispatcher/modbus/modbus_error.hpp
```

`ModbusError` derives from:

```text id="k39ny8"
std::runtime_error
```

## Frame codec

This release adds:

```text id="nqamnx"
ModbusFrameCodec
```

Files:

```text id="m0jejs"
backend/libs/dispatcher-modbus/include/dispatcher/modbus/modbus_frame_codec.hpp
backend/libs/dispatcher-modbus/src/modbus_frame_codec.cpp
```

Supported operations:

```text id="n3jkig"
encode_read_registers_request(...)
decode_read_registers_response(...)
is_exception_response(...)
decode_exception_response(...)
```

The codec supports:

```text id="zuijx6"
Read Holding Registers request encoding
Read Input Registers request encoding
Read Registers response decoding
Modbus exception response detection
Modbus exception response decoding
MBAP protocol id validation
MBAP length validation
byte_count validation
invalid frame rejection
```

Example encoded Read Holding Registers request:

```text id="vikg98"
12 34 00 00 00 06 11 03 00 6B 00 03
```

Meaning:

```text id="dz6vgv"
transaction_id = 0x1234
protocol_id = 0
length = 6
unit_id = 0x11
function_code = 0x03
start_address = 0x006B
quantity = 0x0003
```

## Register decoder

This release adds:

```text id="d8fcrf"
ModbusRegisterDecoder
```

Files:

```text id="rgoxoz"
backend/libs/dispatcher-modbus/include/dispatcher/modbus/modbus_register_decoder.hpp
backend/libs/dispatcher-modbus/src/modbus_register_decoder.cpp
```

Supported value types:

```text id="h6eq1s"
uint16
int16
uint32
int32
float32
```

Supported word orders:

```text id="bpi6n6"
high_word_first
low_word_first
```

Supported operations:

```text id="xyi324"
decode_uint16(...)
decode_int16(...)
decode_uint32(...)
decode_int32(...)
decode_float32(...)
decode_numeric(...)
```

Validation covers:

```text id="jke5pt"
empty register buffers
offset outside buffer
too-small buffer for 32-bit values
```

## Transport abstraction

This release adds a testable transport boundary:

```text id="u4zbuy"
IModbusTcpTransport
```

File:

```text id="q8w7qf"
backend/libs/dispatcher-modbus/include/dispatcher/modbus/modbus_tcp_transport.hpp
```

Transport operation:

```cpp id="mookaf"
[[nodiscard]] virtual ModbusBytes exchange(
    const ModbusBytes& request_frame
) = 0;
```

This allows all client and adapter logic to be tested without a real TCP socket.

The endpoint model was also added:

```text id="ze3d3y"
ModbusTcpEndpoint
```

Default endpoint:

```text id="gh99il"
127.0.0.1:502
```

## ModbusTcpClient

This release adds:

```text id="aaowif"
ModbusTcpClient
```

Files:

```text id="uxo0jp"
backend/libs/dispatcher-modbus/include/dispatcher/modbus/modbus_tcp_client.hpp
backend/libs/dispatcher-modbus/src/modbus_tcp_client.cpp
```

Supported operations:

```text id="v8u13w"
read_holding_registers(...)
read_input_registers(...)
next_transaction_id()
```

The client:

```text id="sve12i"
allocates transaction ids
encodes request frames
calls IModbusTcpTransport
decodes response frames
handles Modbus exception responses
validates response transaction_id
validates response unit_id
validates response function_code
validates expected register count
rejects invalid requests before calling transport
```

## Polling configuration

This release adds the polling configuration model:

```text id="d348bp"
ModbusTagMapping
ModbusPollingConfiguration
ModbusPollRequest
ModbusPollingPlanBuilder
```

Files:

```text id="f4ncy2"
backend/libs/dispatcher-modbus/include/dispatcher/modbus/modbus_polling_config.hpp
backend/libs/dispatcher-modbus/src/modbus_polling_config.cpp
```

`ModbusTagMapping` fields:

```text id="kxzs9o"
tag_id
unit_id
function_code
address
value_type
word_order
scale
offset
enabled
```

`ModbusPollingConfiguration` fields:

```text id="bapirt"
endpoint
poll_interval_ms
timeout_ms
max_registers_per_request
mappings
```

The read-plan builder:

```text id="rfy6br"
validates configuration
skips disabled mappings
sorts enabled mappings
merges adjacent mappings
merges overlapping mappings
does not merge different unit ids
does not merge different function codes
respects max_registers_per_request
rejects duplicate enabled tag ids
rejects invalid register ranges
```

## Telemetry adapter

This release adds:

```text id="p4wb7o"
ModbusTelemetryAdapter
```

Files:

```text id="c44sut"
backend/libs/dispatcher-modbus/include/dispatcher/modbus/modbus_telemetry_adapter.hpp
backend/libs/dispatcher-modbus/src/modbus_telemetry_adapter.cpp
```

Result model:

```text id="od1s4r"
ModbusTelemetrySample
ModbusTelemetryPollError
ModbusTelemetryPollResult
```

The adapter:

```text id="ez6a10"
builds read_plan on construction
executes one polling cycle through poll_once()
reads holding registers
reads input registers
decodes register values
applies scale and offset
returns good-quality samples
records request-level errors
continues polling after one request fails
```

Default sample source:

```text id="s65q5k"
modbus-tcp
```

Default successful quality:

```text id="ashl2u"
good
```

This adapter intentionally returns lightweight Modbus sample DTOs and does not yet push values into `DispatcherRuntime`.

## Simulated transport

This release adds:

```text id="qeeqd6"
ModbusSimulatedTcpTransport
```

Files:

```text id="ml90df"
backend/libs/dispatcher-modbus/include/dispatcher/modbus/modbus_simulated_tcp_transport.hpp
backend/libs/dispatcher-modbus/src/modbus_simulated_tcp_transport.cpp
```

The simulator implements:

```text id="ecspvj"
IModbusTcpTransport
```

Supported operations:

```text id="nexmux"
exchange(...)
set_holding_register(...)
set_input_register(...)
set_holding_registers(...)
set_input_registers(...)
clear()
exchange_count()
requests()
```

The simulator supports:

```text id="d8eghs"
Read Holding Registers
Read Input Registers
missing register exception response
unsupported function exception response
request capture
exchange counting
adapter smoke tests without a network server
```

## Documentation

Modbus adapter documentation was added:

```text id="abm44v"
docs/telemetry/modbus-tcp-adapter-v1.md
```

The document covers:

```text id="t07ilv"
library structure
implemented scope
not-yet-implemented scope
ModbusError
core protocol types
ModbusFrameCodec
ModbusRegisterDecoder
IModbusTcpTransport
ModbusTcpClient
ModbusPollingConfiguration
ModbusTelemetryAdapter
ModbusSimulatedTcpTransport
test coverage
verification commands
known limitations
future integration targets
```

## Test coverage

The Modbus tests are included in:

```text id="px03je"
dispatcher-modbus-tests
```

Test files:

```text id="efozd9"
tests/unit/modbus_frame_codec_tests.cpp
tests/unit/modbus_register_decoder_tests.cpp
tests/unit/modbus_tcp_client_tests.cpp
tests/unit/modbus_polling_config_tests.cpp
tests/unit/modbus_telemetry_adapter_tests.cpp
tests/unit/modbus_simulated_tcp_transport_tests.cpp
```

Expected total backend test executable count:

```text id="scvx04"
14
```

Test coverage includes:

```text id="e6xmpb"
Read Holding Registers request encoding
Read Input Registers request encoding
Read Registers response decoding
Modbus exception detection
Modbus exception decoding
invalid frame rejection
uint16 decoding
int16 decoding
uint32 decoding
int32 decoding
float32 decoding
word order handling
transport abstraction behavior
client response correlation validation
polling configuration validation
read plan merge behavior
telemetry adapter polling
scale and offset
request-level errors
continue after request error
simulated holding register reads
simulated input register reads
simulated exception responses
adapter smoke test through simulator
```

## Verification

Backend configure:

```powershell id="npomqe"
cmake --preset windows-vs-debug
```

Backend build:

```powershell id="u1ybmw"
cmake --build --preset windows-vs-debug
```

Backend tests:

```powershell id="z9qmrv"
ctest --preset windows-vs-debug
```

Expected result:

```text id="ycyie4"
100% tests passed, 0 tests failed out of 14
```

Frontend build:

```powershell id="t0ny2g"
dotnet build frontend\Dispatcher.Frontend.slnx
```

E2E smoke:

```powershell id="btfs7v"
.\out\build\windows-vs-debug\backend\apps\dispatcher-e2e-smoke\Debug\dispatcher-e2e-smoke.exe 10000
```

HTTP server smoke:

```powershell id="bgswcg"
.\out\build\windows-vs-debug\backend\apps\dispatcher-http-server\Debug\dispatcher-http-server.exe 127.0.0.1 18080
```

Manual endpoint checks:

```powershell id="bw3g1r"
Invoke-RestMethod http://127.0.0.1:18080/health
Invoke-RestMethod http://127.0.0.1:18080/ready
Invoke-RestMethod http://127.0.0.1:18080/api/v1/runtime
Invoke-RestMethod http://127.0.0.1:18080/api/v1/alarms
```

## Known limitations

This release is a Modbus protocol and adapter foundation, not a production polling service.

Accepted limitations:

```text id="en0jgj"
No real TCP socket transport exists yet.
No continuous polling loop exists yet.
No DispatcherRuntime integration exists yet.
No telemetry ingest integration exists yet.
No SQLite history persistence integration exists yet.
No frontend configuration screen exists yet.
No HTTP API endpoints expose Modbus adapter state.
No JSON/YAML Modbus configuration import exists yet.
No reconnect/backoff policy exists yet.
No timeout implementation exists in the in-memory transport boundary.
No per-device health state exists yet.
No metrics are emitted yet.
No Modbus write functions exist.
No coil reads exist.
No discrete input reads exist.
No string decoding exists.
No bit field decoding exists.
No byte order variants beyond word order exist yet.
No production TCP security model exists.
```

These limitations are accepted for `v1.5.0-modbus-adapter`.

## Next stage

The next planned stage is:

```text id="yp8l45"
Этап 23 — Notification delivery backend
Tag: v1.6.0-notification-delivery
```

The next stage should focus on actual notification delivery infrastructure while keeping delivery channels isolated from the alarm routing model until they are tested independently.
