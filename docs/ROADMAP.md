# Дорожная карта Dispatcher

Дорожная карта изменяется по мере развития проекта. Количество шагов не является жёстким: шаг можно разделить или объединить, если это уменьшает риск и сохраняет проверяемый результат.

## Обозначения

- `[x]` — содержимое шага подготовлено/реализовано в репозитории.
- `[ ]` — шаг ещё не реализован.
- Финальное принятие каждого технического шага подтверждается пользователем после локальной проверки и отправки нового Git SHA.
- Ошибка проверки означает продолжение текущего шага, а не переход к следующему.

## Phase 0 — Основа проекта

- [x] **S00 — Документация проекта**
  - README;
  - базовая архитектура;
  - архитектурные решения;
  - дорожная карта;
  - правила Web UI;
  - `AGENTS.md` с правилами работы.

## Phase 1 — Первый вертикальный срез: Modbus → Web

- [x] **S01 — Минимальный .NET solution**
  - solution;
  - базовые проекты;
  - версия .NET;
  - зависимости;
  - минимальный automated test;
  - clean build.

- [x] **S02 — Core и TagService**
  - минимальная модель тега;
  - in-memory current values;
  - read/set API;
  - unit-тесты.

- [x] **S03 — Минимальный Modbus TCP read**
  - одно устройство;
  - один Holding Register;
  - преобразование в tag value;
  - тестируемая Modbus-граница.

- [x] **S04 — Polling и состояние устройства**
  - периодический опрос;
  - несколько тегов;
  - timeout/reconnect;
  - connection state;
  - передача результатов в TagService.

- [x] **S05 — ASP.NET Core API**
  - snapshot текущих тегов;
  - состояние устройства;
  - integration tests API.

- [x] **S06 — Blazor WebAssembly read UI**
  - таблица текущих значений;
  - REST initial snapshot;
  - SignalR realtime;
  - connection state;
  - базовый application shell.

- [x] **S07A — Hosted Modbus polling**
  - `Dispatcher.Modbus` подключён к runtime host;
  - временная strongly typed config одного устройства;
  - polling как background service;
  - integration test `Modbus TCP → Core → REST`;
  - автоматическое поступление данных в Web.

- [x] **S07B — Write path Web → Modbus**
  - writable metadata;
  - команда из Web по `TagId`;
  - server-side validation;
  - FC06 запись Holding Register `UInt16`;
  - обновление TagService после успешной записи;
  - компактное отображение command state в Web;
  - end-to-end integration test Web/API → Modbus protocol boundary.

### Результат Phase 1

Рабочая система умеет подключиться к одному настроенному Modbus TCP устройству, читать набор `UInt16` Holding Registers, показывать их в браузере в реальном времени и записывать явно разрешённые значения обратно.

## Phase 2 — Редактор устройств

- [ ] **S08 — Постоянная конфигурация**
  - модель устройств и тегов;
  - SQLite;
  - загрузка конфигурации при старте;
  - разделение configuration/runtime.

- [ ] **S09 — Web-редактор устройств и тегов**
  - список устройств;
  - создать/редактировать/удалить устройство;
  - создать/редактировать/удалить тег;
  - Modbus-настройки;
  - применение изменённой конфигурации;
  - локальная навигация слева;
  - рабочая область в центре;
  - свойства выбранного объекта справа.

### Результат Phase 2

Modbus-устройство и его точки можно полностью настроить через Web без ручного редактирования файлов проекта.

## Phase 3 — SNMP

- [ ] **S10 — SNMP как второй протокол**
  - добавить SNMP-компонент;
  - polling OID;
  - преобразование SNMP-данных в общие теги;
  - интеграция SNMP-настроек в редактор устройств;
  - одновременная работа Modbus и SNMP.

### Результат Phase 3

Система одновременно получает данные по Modbus и SNMP, а Web работает с ними через единую модель тегов.

## Phase 4 — Простая мнемосхема

- [ ] **S11 — Runtime мнемосхемы**
  - модель простой схемы;
  - Text;
  - Rectangle;
  - Value;
  - Indicator;
  - Button;
  - привязка элементов к `TagId`;
  - realtime-отображение;
  - простое управление.

- [ ] **S12 — Минимальный редактор мнемосхемы**
  - создание схемы;
  - добавление/удаление элементов;
  - позиция и размер;
  - свойства;
  - выбор тега;
  - сохранение и загрузка;
  - применение общей компоновки редакторов из `docs/WEB_UI.md`.

### Результат Phase 4

Пользователь может создать простую мнемосхему, связать элементы с тегами Modbus/SNMP, видеть значения в реальном времени и выполнять базовые команды.

## Phase 5 — Дальнейшее развитие

Не детализируется заранее.

После S12 при необходимости планируются:

- historian и тренды;
- alarms/events;
- пользователи и роли;
- дополнительные протоколы;
- шаблоны устройств;
- сложные элементы мнемосхем;
- вычисляемые теги;
- scripting;
- резервирование;
- распределённое исполнение.

Новый функционал добавляется по фактической необходимости.
