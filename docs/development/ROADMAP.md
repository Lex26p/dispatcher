# Дорожная карта проекта «Диспетчер»

## Назначение

Этот файл задаёт направление разработки проекта и используется как рабочая карта для последовательного движения по крупным этапам, спринтам и шагам.

Дорожная карта нужна не для фиксации сроков, а для того, чтобы в каждой рабочей сессии было понятно:

- что сейчас разрабатывается;
- зачем это делается;
- какой результат должен быть получен;
- что не относится к текущей задаче.

Репозиторий является источником истины. Если обсуждение приводит к изменению направления разработки, соответствующее изменение должно быть отражено в этом файле.

---

## Три уровня планирования

### Уровень 1 — этап развития продукта

Крупное законченное направление разработки.

Все этапы первого уровня предварительно определяются заранее. Для каждого этапа фиксируются:

- цель;
- что входит в этап;
- какой результат должен быть получен;
- состояние этапа.

Этап задаёт долгосрочное направление. Изменение состава или порядка уже определённых этапов допускается только при объективной необходимости, выявленной реальной разработкой, а не ради удобства текущей реализации.

### Уровень 2 — спринт

Спринт — законченная часть текущего этапа первого уровня.

Спринты конкретного этапа определяются непосредственно перед началом этого этапа. Агент должен самостоятельно разложить этап на спринты, достаточные для достижения результата этапа, и зафиксировать их в roadmap до начала реализации этапа.

Для текущего `L1-01 — Ядро платформы` спринты уже определены.

Для каждого спринта должны быть определены:

- что именно разрабатывается;
- зачем это нужно текущему этапу;
- ожидаемый результат;
- критерий завершения.

Предпочтительный принцип:

> Один основной сервис — один спринт.

Завершение первого спринта сервиса не означает production-final реализацию. Спринт должен дать рабочую архитектурную основу и необходимый на текущем этапе интерфейс.

### Уровень 3 — шаг

Перед началом конкретного спринта агент обязан сначала разложить его на локальные законченные шаги и зафиксировать их в `docs/development/sprints/<SPRINT>.md`.

План шагов должен быть готов до начала реализации первого шага. Он нужен не только для отчётности, а как рабочее ограничение scope: каждый следующий шаг должен опираться на уже полученный результат и не уводить разработку в сторону.

Если шаги текущего спринта уже зафиксированы, они являются рабочим планом и не переписываются без реальной необходимости.

Шаг должен быть достаточно локальным и законченным, чтобы не требовать постоянного повторного анализа всего проекта.

---

## Правила изменения плана

Дорожная карта является направляющей, но не неизменяемой.

Допускается:

- добавить шаг, если без него невозможно корректно завершить спринт;
- разделить слишком крупный шаг;
- объединить шаги;
- изменить состав будущих спринтов после получения новой информации;
- изменить порядок будущих этапов при объективной необходимости.

При этом изменение самого roadmap, состава этапов или уже согласованного набора спринтов должно быть исключением. Если проблема решается локальной корректировкой шагов текущего спринта без изменения цели, предпочтение отдаётся этому варианту.

Не следует:

- расширять текущий спринт функциями, которые не нужны для его результата;
- добавлять работу только потому, что «раз уж мы здесь, можно сделать ещё и это»;
- детализировать далёкие этапы раньше времени;
- менять цель текущего спринта без явной необходимости;
- менять roadmap только потому, что так проще реализовать текущий код.

Если roadmap всё же меняется, причина и влияние на последующий план должны быть явно отражены в документации.

---

## Отчёты по спринтам и ведение документации

Для каждого спринта создаётся отдельный файл в `docs/development/sprints/`.

Файл создаётся перед стартом спринта и содержит:

- цель;
- ожидаемый результат;
- критерий завершения;
- согласованные шаги.

Sprint-файл и связанная документация ведутся по ходу работы. Когда завершённый шаг фиксирует новую технологию, контракт, архитектурную границу, команды, ограничение или другой факт, необходимый следующим шагам, соответствующие документы обновляются в этом же шаге. Не нужно откладывать такие решения до конца спринта и затем восстанавливать их по переписке.

После завершения в тот же sprint-файл добавляются:

- что фактически реализовано;
- отклонения от плана и причины;
- выполненные проверки;
- известные ограничения;
- итоговый commit SHA.

В конце спринта проводится отдельный targeted documentation audit. Его задача — проверить целостность уже ведущейся документации, найти пропущенную рассинхронизацию между README, roadmap, architecture/contracts, sprint report, service/Web docs и chat context и исправить её до перехода к следующему спринту.

---

# Этапы первого уровня

## L1-01 — Ядро платформы

**Статус:** в разработке.

### Цель

Создать самодостаточную универсальную платформу диспетчеризации, которая способна работать с реальным оборудованием и служить основой для последующих плагинов.

### В этап входит

- общая структура backend на C++;
- Data Hub;
- Event Hub;
- Service Hub;
- Device Manager;
- устройства и метрики;
- записываемые метрики;
- state-метрики;
- диспетчер событий / аварий;
- проекты;
- пользователи и права доступа;
- режим управления;
- Package Manager;
- общий Web UI;
- Dashboard;
- мнемосхемы;
- базовая драйверная инфраструктура;
- драйвер Modbus;
- драйвер SNMP;
- необходимые общие библиотеки проекта.

Modbus и SNMP разрабатываются вместе с ядром, чтобы ядро проверялось на реальном взаимодействии с оборудованием.

### Результат этапа

После завершения этапа система должна позволять:

- подключить оборудование через Modbus и SNMP;
- создать или настроить устройства и метрики;
- получать текущие значения через Data Hub;
- хранить актуальные значения и state-метрики;
- записывать значения в управляемые метрики;
- видеть данные в Web UI;
- использовать Dashboard и мнемосхемы;
- формировать и отображать события и аварии;
- разграничивать доступ пользователей;
- работать с проектами;
- устанавливать и управлять пакетами;
- расширять систему без переработки ядра.

## Спринты L1-01

### CORE-001 — Data Hub

**Статус:** завершён.

**Цель:** создать первый самостоятельный backend-сервис и базовый runtime-механизм текущих значений метрик.

**Результат:** Data Hub отдельно собирается и тестируется, принимает и хранит актуальные значения метрик и state-метрик, отдаёт текущее значение, уведомляет подписчиков и поддерживает базовый путь записи через внешний контракт.

План и итоговый отчёт: [`sprints/CORE-001.md`](sprints/CORE-001.md).

### CORE-002 — Service Hub

**Статус:** завершён.

**Цель:** создать адресное взаимодействие и request/response между сервисами.

**Результат:** сервисы обращаются друг к другу через общий системный механизм без прямых C++ зависимостей между внутренними реализациями. Service Hub предоставляет реальную межпроцессную границу, а минимальный клиентский путь пригоден для использования следующим спринтом Web Shell.

План и итоговый отчёт: [`sprints/CORE-002.md`](sprints/CORE-002.md).

### CORE-003 — Web Shell

**Статус:** завершён.

**Цель:** создать базовую Web-оболочку платформы на React + TypeScript.

**Результат:** существует самостоятельное Web-приложение с компактным global Header, глобальной навигацией, рабочей областью, shared React Service Hub connection и проверенной реальной browser-facing Service Hub integration.

План и итоговый отчёт: [`sprints/CORE-003.md`](sprints/CORE-003.md).

### CORE-004 — Project Manager

**Статус:** завершён.

**Цель:** реализовать проекты как самостоятельные точки консолидации и контекста.

**Результат:** проекты можно создавать, получать, изменять и использовать как контекст платформы без превращения их во владельцев остальных ресурсов.

План и итоговый отчёт: [`sprints/CORE-004.md`](sprints/CORE-004.md).

### CORE-005 — Users & Access

**Статус:** в разработке.

**Цель:** добавить пользовательский контекст и контроль доступа.

**Результат:** backend и Web выполняют действия в контексте пользователя и проверяют права просмотра, управления, редактирования и администрирования, включая режим управления.

План спринта: [`sprints/CORE-005.md`](sprints/CORE-005.md).

### CORE-006 — Device Manager

**Цель:** создать источник истины об устройствах и описании их метрик.

**Результат:** Device Manager знает устройства, метрики, read/write свойства, state-связи и метаданные, а Data Hub остаётся источником текущих runtime-значений.

### CORE-007 — Package Manager

**Цель:** создать единый механизм управления пакетами до реализации реальных протокольных драйверов.

**Результат:** платформа умеет устанавливать, включать, отключать и удалять пакеты и предоставляет базовый Web-интерфейс Package Manager.

### CORE-008 — Driver Runtime / Driver Manager

**Цель:** создать универсальную инфраструктуру протокольных драйверов.

**Результат:** новый драйвер можно реализовать как самостоятельный компонент, подключающийся к Device Manager, Data Hub и Package Manager без изменения их внутренних реализаций.

### CORE-009 — Modbus Driver

**Цель:** реализовать первый реальный протокольный драйвер.

**Результат:** работает реальный поток чтения `Modbus → Driver → Data Hub` и обратный поток записи до Modbus-оборудования.

### CORE-010 — SNMP Driver

**Цель:** реализовать второй реальный протокол и проверить универсальность Driver Runtime.

**Результат:** SNMP-данные становятся обычными метриками платформы, а драйверная архитектура подтверждает независимость от Modbus.

### CORE-011 — Event Hub

**Цель:** создать отдельное пространство обмена событиями и авариями.

**Результат:** события публикуются и доставляются подписчикам независимо от realtime-потока Data Hub.

### CORE-012 — Event Manager

**Цель:** реализовать формирование событий, аварий и state-метрик на основе реальных данных.

**Результат:** изменение метрики может изменить её state, сформировать событие/аварию и отобразиться в Web через Event Hub.

### CORE-013 — System & Administration

**Цель:** включить саму платформу в общую модель мониторинга и добавить базовое администрирование.

**Результат:** CPU, RAM, диск, uptime и состояния сервисов представлены через общие механизмы метрик и событий, а администратор получает необходимые базовые инструменты.

### CORE-014 — Dashboard

**Цель:** создать рабочую основу универсального конструктора пользовательских страниц.

**Результат:** инженер может создать Dashboard из контейнеров и базовых компонентов, использовать параметры/контекст и отображать реальные метрики и state без написания Web-кода.

### CORE-015 — Mimics

**Цель:** реализовать мнемосхемы как самостоятельный SVG-ресурс внутри Dashboard.

**Результат:** можно создать операторский экран с привязками к реальным метрикам, state, динамическими свойствами и управляемыми значениями.

### CORE-016 — Core Integration & Stabilization

**Цель:** проверить завершённость L1-01 как одной работающей платформы.

Это осознанное исключение из правила «один сервис — один спринт»: новый сервис здесь не создаётся.

**Результат:** ядро можно развернуть из чистого состояния, подключить Modbus/SNMP оборудование, создать пользователя, проект, устройство и метрики, получить значения, выполнить разрешённую запись, увидеть state и событие/аварию, использовать Dashboard и мнемосхему и управлять пакетами.

---

## L1-02 — Плагин автоматизации

**Статус:** запланирован.

### Цель

Добавить автоматическую реакцию на данные и события без переноса этой логики в ядро.

### В этап входит

- визуальные правила и сценарии;
- JavaScript как полноценный инструмент сложной логики;
- планировщик;
- запуск по событиям, значениям метрик, расписанию и вручную.

### Результат этапа

Пользователь может создавать автоматическую логику через визуальные средства и JavaScript, а автоматизация работает как устанавливаемый плагин.

### Спринты

Будут определены перед началом этапа.

---

## L1-03 — Плагин истории

**Статус:** запланирован.

### Цель

Добавить долговременное хранение и использование исторических данных без превращения Data Hub в историческую БД.

### Результат этапа

Пользователь может выбирать данные для архивирования, получать историю и использовать её в Dashboard, графиках и других расширениях.

### Спринты

Будут определены перед началом этапа.

---

## L1-04 — Уведомления и пользовательские сообщения

**Статус:** предварительно запланирован.

### Цель

Создать универсальный механизм доставки значимой информации через Web и внешние каналы.

### Результат этапа

Пользователь получает предназначенные ему сообщения через Web и настроенные внешние каналы.

### Спринты

Будут определены перед началом этапа.

---

## L1-05 — Отчёты и аналитика

**Статус:** предварительно запланирован.

### Цель

Добавить формирование отчётных представлений на основе данных платформы.

### Результат этапа

Система формирует полезные отчёты на основе текущих, исторических и событийных данных без переноса предметной логики в ядро.

### Спринты

Будут определены перед началом этапа.

---

## L1-06 — CCTV

**Статус:** предварительно запланирован.

### Цель

Создать отдельный предметный модуль видеонаблюдения поверх общего ядра.

### Результат этапа

Камеры и видеопотоки интегрируются в проекты, Dashboard, права и события, оставаясь отдельным устанавливаемым модулем.

### Спринты

Будут определены перед началом этапа.

---

## L1-07 — СКУД

**Статус:** предварительно запланирован.

### Цель

Создать отдельный модуль контроля и управления доступом.

### Результат этапа

Двери, точки доступа и события проходов используют общие механизмы платформы и остаются отдельным устанавливаемым модулем.

### Спринты

Будут определены перед началом этапа.

---

## L1-08 — Развитие экосистемы пакетов

**Статус:** предварительно запланирован.

### Цель

Расширять платформу преимущественно через пакеты, а не за счёт постоянного роста ядра.

### В направление могут входить

- дополнительные драйверы;
- Dashboard-компоненты;
- библиотеки мнемосхем;
- шаблоны устройств;
- предметные модули;
- интеграционные пакеты.

### Результат этапа

Новые возможности можно добавлять и распространять как пакеты без переработки ядра.

### Спринты

Будут определены перед началом этапа.

---

# Текущая точка

Концепция продукта, архитектурный baseline и рабочие правила разработки зафиксированы.

Спринты `L1-01 — Ядро платформы` определены.

Завершены:

- `CORE-001 — Data Hub`;
- `CORE-002 — Service Hub`;
- `CORE-003 — Web Shell`;
- `CORE-004 — Project Manager`.

Финальный documentation closure `CORE-004`:

`29b1f0ea750633cc53cc4e023585835d2b06ad8b`

`CORE-004` дал отдельный C++ Project Manager, durable local SQLite storage, versioned `project-manager.v1` Service Hub provider, Web list/editor, shared browser-session project context и реальную browser → Service Hub → Project Manager → SQLite restart-recovery integration.

Текущий спринт:

**CORE-005 — Users & Access.**

Plan commit проверен:

`d05cba25981599baaeadd9ad452d1f68dbabd834`

`CORE-005 / Step 1` завершён commit:

`e8b42e69fecf7079f7b18f5f86fe334308d2579c`

Step 1 зафиксировал отдельный C++ Users & Access skeleton, stable user identity, independent `view/control/edit/admin` capabilities, permission sets, global/project assignments, deterministic union-based effective permissions, repository port, unit tests и Linux lifecycle.

`CORE-005 / Step 2` завершён commit `3b140d808638bb0c14a70b2aa1df96eb377af197`: локальный SQLite storage Users & Access, OpenSSL scrypt verifier без plaintext, explicit first-admin bootstrap, durable enabled state/security audit baseline и restart/reopen tests.

`CORE-005 / Step 3` завершён commit `02a2d86e730a6c73ee0c33250bb9d4dc14791681`: зафиксированы `users-access.v1`, opaque server-side session, durable session lifecycle, machine-readable payload schema и authentication/session tests.

`CORE-005 / Step 4` завершён commit `eb5f876e4a35dcbc5b5597e456a1197cc0d9dd1b`: существующий Service Hub v1 совместимо получил optional session `auth` отдельно от business payload, browser `ServiceHubClient` — per-request auth option, а production `users-access.v1` provider — authoritative validation protected session-core operations. Correlation/cancel/timeout semantics и единый browser-compatible transport сохранены.

`CORE-005 / Step 5` завершён commit `ebe98d1f16e55d4024438300c1670ec3a19b1d72`: Project Manager теперь требует session authentication, проверяет permissions через `users-access.v1/evaluate-access`, фильтрует `list-projects`, применяет global `admin` для create и project `view`/`edit`/`admin` для read/update и fail-closed возвращает `project.authorization_unavailable`, если authoritative security dependency недоступна.

Текущий шаг — `Step 6 — Web login, current user и access administration`: Web Shell должен получить реальный authenticated user context, login/logout/current-user UX, session restoration/invalid-session handling и минимальный administration UI для users, permission sets и assignments поверх shared Service Hub connection.

План CORE-005 сознательно ограничивает первый real enforcement существующими global/project scopes и Project Manager. Device/Dashboard-specific ACL, внешние identity providers, MFA, Event Hub audit publication и production deployment security не добавляются раньше соответствующих реальных требований.
